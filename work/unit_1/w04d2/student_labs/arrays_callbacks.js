const iceCream = ['Vanilla','Chocolate','Strawberry','Rocky Road'];

const updateIceCreams = iceCream.map((flavor) => {
    return flavor + " Ice Cream"

})

console.log(updateIceCreams);

// creat array  that multiplies each number in the array by 2
const numberArray = [2,4,6,8,10];

const newNumberArray = numberArray.map((num) => {
    return num * 2
})
console.log(newNumberArray);

// activity

const classArray = ['Javascript','HTML','CSS','Data Analysis', 'Marketing', 'Database Design', 'Visual Design'];

const numberArray =  [31, 203, 30, 84, 5, 62, 770, 8, 99, 10, 0];
